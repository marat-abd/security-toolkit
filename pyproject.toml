[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "security-toolkit"
version = "0.1.0"
description = "Lightweight Python toolkit for basic security assessment and network diagnostics"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
